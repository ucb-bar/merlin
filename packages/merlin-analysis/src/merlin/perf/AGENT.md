# Performance analysis

Owns the optional recovery-analysis implementation and `merlin-recovery` command.
Preserve the `merlin.perf.recovery` import identity and historical evidence semantics.
Core owns `merlin.perf.__init__`; do not add a competing namespace initializer here.
Generic performance contracts and observation types remain in core.
