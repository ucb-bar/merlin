# AGENT.md — merlin/python/merlin/design_pressure/ingest

## Purpose

Ingest workload regions from external frontends (e.g. model2MLIR linalg-on-tensors).

## Modules

- `mlir_m2m.py` — Extract a workload region from model2MLIR (linalg-on-tensors) MLIR.

<!-- Purpose/Modules derived from docstrings via build_tools/scripts/gen_package_docs.py.
     Add hand-written notes (invariants, gotchas) below. -->
