# Package tests

Use harmless local subprocess fixtures for execution and resume. Never start paid
agents or hardware during tests. Read legacy argparse declarations statically when
checking adapter flags; importing those engines can have side effects.
