# Optional experiments modules in the stable merlin namespace

The core owns merlin/__init__.py and extends its package path. Keep one module
identity across core and experiments; do not add a competing initializer here.
