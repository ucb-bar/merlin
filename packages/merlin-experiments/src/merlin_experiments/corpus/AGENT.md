# AGENT.md — packages/merlin-experiments/src/merlin_experiments/corpus

`preparation.py` verifies derivation receipts, assembles complete source pools and
stages declared resources. `release.py` owns explicit prepare/inspect/reviewed seal
and snapshot verification. `admission.py` observes actual evaluator construction
for conformance and public-cohort selection; advertised tiers are not evidence.

These are host-only workflows, not candidate tools or numerical implementations.
Keep private copies on independent inodes, public summaries aggregate-only, and
human approval explicit. Preserve schemas, historical provenance identities and
adapter construction order. Core owns copying primitives, admission validation,
coverage and leases. Importing this package must not import graders or run work.
New reviewed bundle generation explicitly selects the installed compiler source root
for public Python tools and their deny surfaces. Target resources remain descriptor-owned;
do not invent a checkout alias or relabel a baseline bundle as an assisted treatment.

`numeric_policy.py` loads only an explicitly declared authored recipe and projects
its complete numerical assumptions. It never discovers target-named profiles or
merges generated/private sidecars. Frozen callers resolve the file through their
existing snapshot verifier before loading; this reader creates no seal. Its byte
provenance identifies declared assumptions, not hardware-verified semantics.
