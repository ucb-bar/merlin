"""Public standalone Phase 1 clients, copied individually into candidate workspaces.

The tool registry owns staged filenames. Importing this package starts no tools;
clients use only the standard library and never import their host-side brokers.
"""
