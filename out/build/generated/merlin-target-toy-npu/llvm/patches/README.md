# LLVM patch series — toy_npu

Reviewable patch files against a pinned `third_party/llvm-project` go here, with a `series` file. **None during the MVP** — do not fork LLVM before target-dialect + simulator validation passes.
