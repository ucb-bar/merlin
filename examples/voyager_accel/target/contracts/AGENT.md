# Voyager target residual

This draft residual is an authored target input, not a generated or qualified target contract.
The historical path remains a compatibility symlink for existing experiment descriptors.
