# Universal target declarations

`descriptor.yaml` and `contracts/` are authored inputs. The ABI header is the
attested public configuration-specific header; it is not generated by Merlin.
The historical `merlin/targets/gemmini_universal` paths are compatibility links.
Do not promote the external RTL symlink or archived bundles as portable inputs.
