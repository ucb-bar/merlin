# Gemmini Universal example

This is a separate hardware configuration, not an alias for `examples/gemmini`.
Own authored public target declarations and experiment selection here. Keep frozen
receipts and generated bundles at their historical paths; new run products belong
under the configured `out/` root. Never copy another user's Chipyard/RTL checkout,
private grading inputs, or the Gemmini reference backend into this example.
