# Authored prompt inputs

These are public source prompts, not generated run artifacts. Preserve experimental
contracts when editing them. The runner archives the final served `TASK.md` in each
run; generated-prompt modes use shared rendering instead of these legacy templates.
