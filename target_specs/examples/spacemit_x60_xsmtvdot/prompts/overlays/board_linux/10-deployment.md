---
section: implementation_focus
merge: append
---
The `{{ deployment.name }}` overlay is a board-oriented Linux deployment. Keep board transport and packaging details in deployment outputs, not in the canonical hardware capability spec.
