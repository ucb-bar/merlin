---
section: implementation_focus
merge: append
---
The `{{ deployment.name }}` overlay is a FireSim-backed accelerator deployment. Keep Chipyard and FireSim pins, runtime assumptions, and recipe metadata inside the deployment layer.
