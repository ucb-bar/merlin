---
section: implementation_focus
merge: append
---
The `{{ deployment.name }}` overlay targets FireSim on U250. Preserve simulator-specific recipe metadata and build-profile facts as deployment-only data, and keep the planner ready for future non-Chipyard deployment overlays.
