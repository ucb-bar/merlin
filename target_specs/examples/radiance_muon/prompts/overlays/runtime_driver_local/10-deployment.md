---
section: implementation_focus
merge: append
---
The `{{ deployment.name }}` overlay represents a local runtime-driver setup. Keep URI schemes, backend IDs, and smoke-test assumptions in deployment and runtime artifacts instead of hardcoding them into compiler-target logic.
