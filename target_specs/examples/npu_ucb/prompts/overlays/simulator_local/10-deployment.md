---
section: implementation_focus
merge: append
---
The `{{ deployment.name }}` overlay is simulator-local. Treat simulator availability as a deployment resource that strengthens verification planning without pretending it is equivalent to board or hardware validation.
