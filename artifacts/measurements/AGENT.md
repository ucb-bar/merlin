# AGENT.md — artifacts/measurements

## Purpose

K1 board measurement campaigns (scripts/k1_*.py): cross-framework/op/e2e on real hardware.

## Invariants

- Contents are gitignored; only AGENT.md / README.md / .gitkeep are tracked.
- Created via `merlin.common.artifacts` (start_run / new_product / cache_dir), never hand-built paths.
- Axis: **model/workload (measured on K1)**.
