"""Implementation package for the `quantize` subcommand (`./merlin quantize`).

The registered shim lives at `tools/quantize/cli.py`. This package will hold
quantization helpers; the legacy `quantize/int8.py` module moves here in
Phase 2 as `quantize.int8`.
"""
