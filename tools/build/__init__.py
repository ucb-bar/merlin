"""Implementation package for the `build` subcommand (`./merlin build`).

The CLI entry point lives in `cli.py`. Future split will add presets.py
(PROFILE_PRESETS dict), cmake.py (helpers), packaging.py, radiance.py.
"""
