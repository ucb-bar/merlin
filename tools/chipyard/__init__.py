"""Implementation package for the `chipyard` subcommand (`./merlin chipyard`).

The CLI entry point lives in `cli.py`. Future split will add config.py,
recipe.py, git_ops.py, bare_metal.py, firesim.py, zephyr.py, radiance.py,
status.py — one per logical subsystem.
"""
