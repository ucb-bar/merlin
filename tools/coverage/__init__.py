"""Implementation package for the `coverage-check` subcommand (`./merlin coverage-check`).

The registered shim lives at `tools/coverage/cli.py`. The legacy
`coverage/check.py` module moves here in Phase 2 as `coverage.check`. Phase
3d adds `coverage.audit` (from `coverage/audit.py`).
"""
