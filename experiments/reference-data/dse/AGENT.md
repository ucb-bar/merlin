# DSE reference analysis evidence

`case_study/` is an immutable snapshot consumed through `merlin.dse_guidance.reference_data`.
`MIGRATION.json` records the original path and SHA-256 of every moved payload file.
Changes to payload require an explicit reviewed reference update, not formatting or regeneration.
