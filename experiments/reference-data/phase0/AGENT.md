# Retained Phase 0 synthesis references

These are byte-preserved public experiment inputs, not generator output destinations.
Never regenerate them in place or treat them as proof of current synthesis parity.
Some contain manually reviewed policy overrides. Preserve historical provenance.
New synthesis belongs under the configured artifact root; examples select inputs explicitly.
No private holdouts, goldens, weights or runtime state belong here.
