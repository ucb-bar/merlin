# abc4 experiment archive — DO NOT DELETE
Self-contained durable record of the abc4 three-arm run (baseline-C++ / merlin-xDSL / merlin+CIRCT):
per-arm qa_loop_state, selfcheck_log, qa_history (incl. rtl_checks), round transcripts, the FROZEN
submissions, environment.yaml; plus the full analysis (abc4_analysis/) and the authoritative verilator
audits. This is the evidence behind the 25/25 results + the timing/cost/CIRCT analysis. Preserve it.
The runs/ dirs are the live source; this archive is the frozen copy in case _qa_ws or runs/ churn.
