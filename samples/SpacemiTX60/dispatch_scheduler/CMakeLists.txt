# Legacy alias — the canonical home for merlin-dispatch-scheduler is now
# samples/common/dispatch_scheduler/. Leaving this directory present for
# back-compat references; nothing builds here anymore. (SpacemiTX60-only logic,
# if any, would still live here.)
