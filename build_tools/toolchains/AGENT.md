# Toolchain qualification

Record independently qualified compiler environments here. Do not silently replace
Python MLIR bindings with a command-line tool: the compiler executes Python IR rewrites.
Pins describe tested environments, not permission to upgrade a shared installation.
