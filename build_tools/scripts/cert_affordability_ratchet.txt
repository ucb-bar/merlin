# Capsules that demand L3 at a size over the affordability budget while declaring neither
# `max_oracle_tier: L2` nor `extends`. THIS FILE MAY ONLY SHRINK.
#
# Measured 2026-09-02: build_tools/scripts/check_cert_affordability.py --budget-s 900.
# Cost law from four cycle-accurate calibration runs at 512/1024/2048/4096 written elements
# (177.2 / 351.6 / 723.1 / 1682.6 s): seconds = 0.20509 * output^1.0782, log-log r2 0.9976,
# every point within 5.4%. The law is SUPERLINEAR -- s/element runs 0.346, 0.343, 0.353, 0.411 --
# so a flat rate under-predicts by 16% at the top rung and by 57% at the largest capsule here.
#
# 295 capsules demand L3 for a predicted 181.3h.
# 9 of them are beyond the calibrated 4,096 elements and carry
# 163.21h of that -- so most of the total rests on extrapolation, stated here
# rather than implied.
#
# The twelve below are 164.5h of it. The
# remedy is a DECLARATION, not a smaller shape: the large shape is usually the representative one,
# so pair each with an affordable certified sibling and cap this one at L2 via `extends` -- the
# split conformance.py already derives and only three capsules use.
# Predicted total once these twelve are capped at L2: 16.7h

SY_regime_spills    # 142,631s, 262,144 written elements EXTRAPOLATED
SY_model_tiny_llama    # 139,030s, 256,000 written elements EXTRAPOLATED
OPM0_ffn_up    # 106,945s, 200,704 written elements EXTRAPOLATED
OPM2_attn_proj    # 78,424s, 150,528 written elements EXTRAPOLATED
SY_regime_fits_single    # 67,553s, 131,072 written elements EXTRAPOLATED
OPM1_ffn_down    # 23,989s, 50,176 written elements EXTRAPOLATED
OPM3_patch_embed    # 23,989s, 50,176 written elements EXTRAPOLATED
AHD00_deep_epilogue_hidden_96x64x64    # 2,493s, 6,144 written elements EXTRAPOLATED
AHD01_deep_epilogue_hidden_96x160x64    # 2,493s, 6,144 written elements EXTRAPOLATED
PL01_k32    # 1,610s, 4,096 written elements
PL03_k64    # 1,610s, 4,096 written elements
SY_micro_model    # 1,610s, 4,096 written elements
