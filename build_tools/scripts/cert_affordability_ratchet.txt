# Capsules that demand L3 at a size over the affordability budget while declaring neither
# `max_oracle_tier: L2` nor `extends`. THIS FILE MAY ONLY SHRINK.
#
# Measured 2026-09-02: build_tools/scripts/check_cert_affordability.py --budget-s 900.
# Cost law from four cycle-accurate calibration runs at 512/1024/2048/4096 written elements
# (177.2 / 351.6 / 723.1 / 1682.6 s): seconds = 0.20509 * output^1.0782, log-log r2 0.9976,
# every point within 5.4%. SUPERLINEAR -- s/element runs 0.346, 0.343, 0.353, 0.411 -- so a flat
# rate under-predicts by 16% at the top rung and 57% at the largest capsule listed here.
#
# 295 capsules demand L3 for a predicted 181.3h.
# 9 are beyond the calibrated 4,096 elements and carry 163.21h
# of that, so most of the total rests on extrapolation -- stated, not implied.
#
# TWO KINDS OF DEBT, because they have different remedies.

# (1) CAPPABLE AT L2 (10 capsules, 163.7h). Purely
# functional: pair each with an affordable certified sibling and cap this one at L2 via `extends`.
# The large shape is usually the representative one, so the remedy is a DECLARATION, not a smaller
# shape. conformance.py already derives that split; only three capsules use it.
# Predicted total once these are capped: 17.6h, keeping 285 of 295 certifications.

SY_regime_spills    # 142,631s, 262,144 written EXTRAPOLATED
SY_model_tiny_llama    # 139,030s, 256,000 written EXTRAPOLATED
OPM0_ffn_up    # 106,945s, 200,704 written EXTRAPOLATED
OPM2_attn_proj    # 78,424s, 150,528 written EXTRAPOLATED
SY_regime_fits_single    # 67,553s, 131,072 written EXTRAPOLATED
OPM1_ffn_down    # 23,989s, 50,176 written EXTRAPOLATED
OPM3_patch_embed    # 23,989s, 50,176 written EXTRAPOLATED
AHD00_deep_epilogue_hidden_96x64x64    # 2,493s, 6,144 written EXTRAPOLATED
AHD01_deep_epilogue_hidden_96x160x64    # 2,493s, 6,144 written EXTRAPOLATED
SY_micro_model    # 1,610s, 4,096 written

# (2) CANNOT BE CAPPED (2 capsules, 0.9h). A
# performance capsule whose gate instrument is a cycle count NEEDS a cycle-accurate tier; capping
# it at L2 would delete the measurement it exists to take. Its remedy is a smaller shape or an
# accepted cost. Listed so nobody 'fixes' them the other way.

PL01_k32    # 1,610s, 4,096 written, PL: cycle_count_and_preflight
PL03_k64    # 1,610s, 4,096 written, PL: cycle_count_and_preflight
