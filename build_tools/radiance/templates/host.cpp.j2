// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
// AUTOGENERATED host stub for the {{ kernel_name }} Muon kernel.
//
// On RadianceMuonConfig (and other host-less Radiance configs) this host
// is unused — the simulator harness loads the kernel.radiance.elf directly.
// The stub is kept for symmetry with the upstream radiance-kernels build
// (which produces a fused .soc.elf that embeds the RV32 kernel into an
// RV64 host program for SingleClusterConfig-style runs).

int main() {
  return 0;
}
