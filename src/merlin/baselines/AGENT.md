# AGENT.md — src/merlin/baselines

Compatibility names for shared capture, toolchain discovery, and ISA audit modules.
Their implementations live in capture/common/runtime. Real baseline frameworks belong in
packages/merlin-analysis. Keep optional baseline contract exports lazy.
