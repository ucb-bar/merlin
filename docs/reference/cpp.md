# C/C++ API

The C/C++ reference is generated with Doxygen and rendered through `mkdoxy` from:

- `compiler/src/merlin`
- `compiler/plugins`
- `samples`
- `benchmarks`

Use this page as the C/C++ API entrypoint once the `mkdoxy` project output is built.
