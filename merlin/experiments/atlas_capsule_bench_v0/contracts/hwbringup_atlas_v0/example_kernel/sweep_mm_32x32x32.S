# sweep_mm_32x32x32.S — cycle-only single-MXU0 tiled matmul
# Auto-generated by gen_mm_sweep.py  (M=32 N=32 K=32; tiles 1x1x1, 1 output tiles, 1 VMATMULs)
# Region-isolated: CSRR 0xC00 (atlas cycle counter) brackets the
# VMATMUL issue+drain window; delta banked to CSR_DBG1. Boot excluded.
# @TIMEOUT 4000000
# @DRAM_BASE 0x90000000
# @PERF_REPORT
# @PERF_MACS_MXU0 32768

    LI    x5, 0x00000000
    DMA.CONFIG x5, 0
    LI    x12, 1024
    SELI  0, 127
    ADDI  x6, x0, 0
    VLOAD 0, x6, 0
    DELAY 33
    VLOAD 1, x6, 0
    DELAY 33
    VMATPUSH.W.MXU0   0, 1
    DELAY 31
    VMATPUSH.W.MXU0   1, 1
    DELAY 31

    CSRW  x0,  0xC20                # reset mxu busy counters
    CSRR  x20, 0xC00                # start cycle snapshot
    VMATMUL.MXU0      0, 0, 0
    DELAY 62
    VMATPOP.FP8.MXU0  2, 0, 0
    CSRR  x21, 0xC00                # end cycle snapshot
    SUB   x22, x21, x20
    CSRW  x22, 0xC11                # bank region delta -> CSR_DBG1

    LI    x30, 1
    CSRW  x30, 0xC10
    ECALL
