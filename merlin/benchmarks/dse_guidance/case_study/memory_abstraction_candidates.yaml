# memory_abstraction_candidates (structural)
memory_abstraction_candidates:
  candidates:
  - abstraction: resident_weight_object
    evidence: recovered_from_ir
    implied_by_n_objects: 5
  - abstraction: resident_activation_object
    evidence: recovered_from_ir
    implied_by_n_objects: 5
  note: memory abstractions implied by the reuse/residency structure. Counts are how many (workload,object) pairs imply each. Structural — no bandwidth/speedup claim.
  unavailable_abstractions:
  - abstraction: prefix_kv_object
    reason: attention/KV lowered into matmuls
  - abstraction: scale_sideband_object
    reason: scales erased (dequantized capture)
  - abstraction: packed_weight_store
    reason: weights stored f32 (no packed layout)
