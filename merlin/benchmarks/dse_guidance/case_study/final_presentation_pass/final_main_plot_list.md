# Final MAIN plots (on the slides)

12 plots. Generated by `_gen_inventory.py`.

## table_capture_summary
- **DSE question:** What does Merlin actually recover from a capture?
- **Say (claim):** Merlin recovers a loop/region/state contract (K, repeated ops, carried state, KV), not just op shapes.
- **Evidence tier / scale:** A / structural  ·  **source:** `loop_aware_contract.csv`  ·  restyled (clean) ✓
- **Required caveat:** structural facts; no performance implied
- **Avoid saying:** speedup / faster / cycles / area / energy / throughput / optimal / measured hardware performance

## capture_fidelity
- **DSE question:** Which DSE axes does the capture enable vs block?
- **Say (claim):** Recovered shapes/roles/K/KV vs erased low-bit vs not-claimed latency — fidelity gates the axes.
- **Evidence tier / scale:** A/B / structural  ·  **source:** `IM.capture_fidelity`  ·  restyled (clean) ✓
- **Required caveat:** 'blocked/erased' is a capture limit, not a workload property
- **Avoid saying:** speedup / faster / cycles / area / energy / throughput / optimal / measured hardware performance

## capture_level_ablation
- **DSE question:** What does each capture level unlock?
- **Say (claim):** flat→high_level→quant_qdq progressively names attention/norm then low-bit.
- **Evidence tier / scale:** A / structural  ·  **source:** `IM.capture_level_ablation`  ·  restyled (clean) ✓
- **Required caveat:** counts of named ops; structural
- **Avoid saying:** speedup / faster / cycles / area / energy / throughput / optimal / measured hardware performance

## primitive_set_frontier
- **DSE question:** Is one tiling primitive enough across workloads?
- **Say (claim):** One primitive is not robust; a 2-set reaches near-full worst-case coverage.
- **Evidence tier / scale:** A / structural  ·  **source:** `IM.primitive_set_frontier`  ·  restyled (clean) ✓
- **Required caveat:** structural pad-waste coverage; not hardware performance
- **Avoid saying:** speedup / faster / cycles / area / energy / throughput / optimal / measured hardware performance

## operator_cumulative_mac
- **DSE question:** Is compute hot-op-dominated or diffuse?
- **Say (claim):** Workloads split into hot-op-dominated (few giant ops) vs diffuse regimes.
- **Evidence tier / scale:** A / structural  ·  **source:** `operator_shape_table.csv`  ·  restyled (clean) ✓
- **Required caveat:** cumulative MAC share from IR shapes
- **Avoid saying:** speedup / faster / cycles / area / energy / throughput / optimal / measured hardware performance

## decision_weight_residency
- **DSE question:** How does weight traffic scale with the loop?
- **Say (claim):** Non-resident weight traffic grows ×K; residency loads once; dot = the model's IR K.
- **Evidence tier / scale:** A / captured-config  ·  **source:** `data_movement_table.csv`  ·  restyled (clean) ✓
- **Required caveat:** weight BYTES MOVED (not bandwidth); captured-config scale
- **Avoid saying:** speedup / faster / cycles / area / energy / throughput / optimal / measured hardware performance

## decision_capacity_dtype
- **DSE question:** When do repeated-head weights fit on-chip?
- **Say (claim):** dtype sets the residency budget: int4/int8 fit more workloads at a given on-chip capacity.
- **Evidence tier / scale:** A / captured-config  ·  **source:** `dtype_capacity_table.csv`  ·  restyled (clean) ✓
- **Required caveat:** captured-config weight sizes; feasibility envelope
- **Avoid saying:** speedup / faster / cycles / area / energy / throughput / optimal / measured hardware performance

## realtime_requirement
- **DSE question:** What weight bandwidth must a machine provide for 30 Hz?
- **Say (claim):** Residency lowers the required weight bandwidth to hit the target rate.
- **Evidence tier / scale:** A/B / deployment-composition  ·  **source:** `realtime_requirement.csv`  ·  restyled (clean) ✓
- **Required caveat:** requirement floor under the workload model; not a chip measurement
- **Avoid saying:** speedup / faster / cycles / area / energy / throughput / optimal / measured hardware performance

## lever_ablation
- **DSE question:** How do system levers lower the 30 Hz requirement?
- **Say (claim):** Action chunking (/H) then residency (/K) each cut the required weight bandwidth.
- **Evidence tier / scale:** A/B / deployment-composition  ·  **source:** `arithmetic_intensity.csv`  ·  restyled (clean) ✓
- **Required caveat:** requirement reduction (H,K from source/config); not a speedup
- **Avoid saying:** speedup / faster / cycles / area / energy / throughput / optimal / measured hardware performance

## boundary_necessity_matrix
- **DSE question:** Which abstractions should DSE search first?
- **Say (claim):** A few abstractions are necessary across all workloads; others are workload-specific or blocked.
- **Evidence tier / scale:** B / structural  ·  **source:** `IM.abstraction_necessity`  ·  restyled (clean) ✓
- **Required caveat:** 'blocked' = capture/evidence blocked; 'possible' = not discriminating
- **Avoid saying:** speedup / faster / cycles / area / energy / throughput / optimal / measured hardware performance

## arithmetic_intensity_roofline
- **DSE question:** How does residency shift intensity across machine-balance regimes?
- **Say (claim):** Residency raises weight-stream AI, shifting workloads across a hypothetical balance band.
- **Evidence tier / scale:** A/B / deployment-composition  ·  **source:** `arithmetic_intensity.csv`  ·  restyled (clean) ✓
- **Required caveat:** weight-stream AI; hypothetical balance; modeling view, not measured performance, not full-memory AI
- **Avoid saying:** speedup / faster / cycles / area / energy / throughput / optimal / measured hardware performance

## visible_linear_fraction
- **DSE question:** How much recovered work is linear-GEMM vs attention?
- **Say (claim):** Most workloads are GEMM-geometry dominated; smolVLA is attention-heavy.
- **Evidence tier / scale:** A / structural  ·  **source:** `work_coverage_table.csv`  ·  restyled (clean) ✓
- **Required caveat:** linear/(linear+attention); excludes erased/unmodeled work
- **Avoid saying:** speedup / faster / cycles / area / energy / throughput / optimal / measured hardware performance

