# Final Plot Classification

39 plots. Generated by `_gen_inventory.py` (reproducible).

| plot_id | decision | dse_question | safe_takeaway | required_caveat |
|---|---|---|---|---|
| table_capture_summary | main | What does Merlin actually recover from a capture? | Merlin recovers a loop/region/state contract (K, repeated ops, carried state, KV), not just op shapes. | structural facts; no performance implied |
| capture_fidelity | main | Which DSE axes does the capture enable vs block? | Recovered shapes/roles/K/KV vs erased low-bit vs not-claimed latency — fidelity gates the axes. | 'blocked/erased' is a capture limit, not a workload property |
| capture_level_ablation | main | What does each capture level unlock? | flat→high_level→quant_qdq progressively names attention/norm then low-bit. | counts of named ops; structural |
| primitive_set_frontier | main | Is one tiling primitive enough across workloads? | One primitive is not robust; a 2-set reaches near-full worst-case coverage. | structural pad-waste coverage; not hardware performance |
| operator_cumulative_mac | main | Is compute hot-op-dominated or diffuse? | Workloads split into hot-op-dominated (few giant ops) vs diffuse regimes. | cumulative MAC share from IR shapes |
| decision_weight_residency | main | How does weight traffic scale with the loop? | Non-resident weight traffic grows ×K; residency loads once; dot = the model's IR K. | weight BYTES MOVED (not bandwidth); captured-config scale |
| decision_capacity_dtype | main | When do repeated-head weights fit on-chip? | dtype sets the residency budget: int4/int8 fit more workloads at a given on-chip capacity. | captured-config weight sizes; feasibility envelope |
| realtime_requirement | main | What weight bandwidth must a machine provide for 30 Hz? | Residency lowers the required weight bandwidth to hit the target rate. | requirement floor under the workload model; not a chip measurement |
| lever_ablation | main | How do system levers lower the 30 Hz requirement? | Action chunking (/H) then residency (/K) each cut the required weight bandwidth. | requirement reduction (H,K from source/config); not a speedup |
| boundary_necessity_matrix | main | Which abstractions should DSE search first? | A few abstractions are necessary across all workloads; others are workload-specific or blocked. | 'blocked' = capture/evidence blocked; 'possible' = not discriminating |
| arithmetic_intensity_roofline | main | How does residency shift intensity across machine-balance regimes? | Residency raises weight-stream AI, shifting workloads across a hypothetical balance band. | weight-stream AI; hypothetical balance; modeling view, not measured performance, not full-memory AI |
| visible_linear_fraction | main | How much recovered work is linear-GEMM vs attention? | Most workloads are GEMM-geometry dominated; smolVLA is attention-heavy. | linear/(linear+attention); excludes erased/unmodeled work |
| work_coverage_by_workload | backup | Linear-GEMM vs attention MAC mass per workload? | Per-workload split of recovered linear vs attention MACs. | captured-config; not deployment scale |
| deployment_magnitude | backup | Deployment params/MACs scale per workload? | Deployment magnitudes by config-composition (embed + per-layer × real n_layers). | deployment-composition; small_llama (synthetic) omitted |
| sharding_scalability | backup | What is the transfer cost of parallelism? | Extra comm bytes per unit output rise with PU count; M broadcast > K reduction > N. | structural communication bytes; not a performance result |
| sharding_comm_tradeoff | backup | Absolute comm cost vs shard count? | Absolute extra communication (GB) by axis × shard count. | structural bytes; not performance |
| decision_sharding_per_top_op | backup | Which top-MAC ops are expensive to shard? | Sharding extra bytes for the top-MAC ops, normalized by output bytes. | captured-config bytes |
| shape_class_mac_share | backup | MAC distribution across GEMM shape classes? | Per-workload MAC split by shape class (square/skinny/...). | captured-config |
| primitive_frontier_by_threshold | backup | Is the primitive-set frontier robust to the pad-waste threshold? | Worst-workload coverage vs set size across 5/10/20% thresholds. | structural coverage |
| macro_vs_micro_primitive_coverage | backup | Macro vs micro vs worst coverage by set size? | Coverage measured three ways across set size. | structural coverage |
| resident_capacity_by_dtype | backup | Resident weight bytes per region by dtype? | Resident weight bytes per region, int8 vs bf16. | captured-config scale |
| required_compute_envelope | backup | Required compute vs replan deadline? | Required GMAC/s as a function of deadline (a requirement). | requirement, not measured performance; configured K |
| required_memory_movement_envelope | backup | Required weight bandwidth @100ms, resident vs reload? | Residency removes a K× factor from the required weight bandwidth. | requirement floor; not a chip |
| critical_path_parallelism | backup | How much inter-op parallelism is available? | work/span per workload (low ⇒ mostly serial chains; parallelism is intra-op). | structural dependency graph |
| table_deployment_magnitudes | backup | Exact deployment magnitudes? | Layers, params, MACs/token by config-composition; openVLA/tiny_llama are exact anchors. | deployment-composition |
| table_arithmetic_intensity | backup | Exact arithmetic intensity + residency gain? | Weight-stream AI resident vs reload + residency gain per workload. | weight-stream (modeling) AI, not full-memory; not measured performance |
| table_low_bit_tiers | backup | Low-bit tier per workload? | native/qdq_int8/dequant_only per workload; int8 ratified by the measured gate. | fp8/int4 never assumed |
| table_realtime_requirement | backup | Exact real-time requirement per regime? | Required compute + weight bandwidth per VLA/VLM regime. | requirement floor; not a chip |
| measurement_priority_bar | backup | What missing measurement unblocks the most candidates? | Ranks missing measurements by how many abstraction candidates they unblock. | meta/methodology view |
| workload_influence_loo_delta | backup | How stable are corpus metrics to leave-one-out? | Leave-one-out stability of corpus metrics (winner-stable vs magnitude-unstable). | robustness diagnostic |
| evidence_type_by_workload | QA | Provenance mix of facts per workload? | Fact counts by evidence tier per workload. | QA/methodology only |
| evidence_type_by_phase | QA | Provenance mix of facts per phase? | Fact counts by evidence tier per analysis phase. | QA/methodology only |
| required_command_rate_envelope | QA | Required dispatch/s vs deadline? | Required dispatch rate (a PROXY). | PROXY ~12× undercount; measured only for small_llama |
| primitive_regret_bar | QA | Per-primitive mean vs worst coverage? | Redundant with primitive_set_frontier / decision_primitive_choice. | structural coverage |
| decision_primitive_choice | QA | Single-primitive worst vs mean coverage? | Subsumed by primitive_set_frontier for the talk. | structural coverage |
| avoidable_reload_by_region | QA | Avoidable reload bytes per region? | Subsumed by decision_weight_residency. | captured-config bytes |
| boundary_placement_heatmap | QA | Boundary placement per abstraction × level? | Raw numeric-colored version; replaced by boundary_placement_simplified (categorical). | use the simplified categorical version for the talk |
| realtime_requirement_surface | drop | 3D required compute vs rate × workload? | 3D is hard to read; the 2D realtime_requirement + lever_ablation carry the message. | backup-only; requirement floor, not a chip |
| decision_sharding_cost | drop | Absolute extra sharding bytes? | Log axis wastes space; superseded by sharding_scalability (normalized). | backup-only; structural bytes |
