# parallel_region_candidates (structural; no speedup)
parallel_region_candidates:
  note: inter-operator concurrency from the recovered data-dependency DAG. available_parallelism = total_work / critical_path_work (work/span, average parallelism) — a STRUCTURAL property, NOT a speedup and assuming no hardware. independent_components are op groups with no data dependency between them.
  workloads:
  - available_parallelism:
      evidence: derived_requirement
      value: 1.1107
    independent_components:
      evidence: recovered_from_ir
      value: 1
    interpretation: deep sequential dependency chain — the parallelism opportunity is intra-op sharding, not inter-op concurrency
    max_ready_width:
      evidence: recovered_from_ir
      value: 4
    serialization: mostly_sequential
    workload: rdt
  - available_parallelism:
      evidence: derived_requirement
      value: 1.215
    independent_components:
      evidence: recovered_from_ir
      value: 1
    interpretation: deep sequential dependency chain — the parallelism opportunity is intra-op sharding, not inter-op concurrency
    max_ready_width:
      evidence: recovered_from_ir
      value: 3
    serialization: mostly_sequential
    workload: openvla
  - available_parallelism:
      evidence: derived_requirement
      value: 1.5597
    independent_components:
      evidence: recovered_from_ir
      value: 1
    interpretation: some independent operators per level — modest inter-op concurrency exists
    max_ready_width:
      evidence: recovered_from_ir
      value: 3
    serialization: some_parallelism
    workload: small_llama
  - available_parallelism:
      evidence: derived_requirement
      value: 1.1959
    independent_components:
      evidence: recovered_from_ir
      value: 1
    interpretation: deep sequential dependency chain — the parallelism opportunity is intra-op sharding, not inter-op concurrency
    max_ready_width:
      evidence: recovered_from_ir
      value: 3
    serialization: mostly_sequential
    workload: tiny_llama
