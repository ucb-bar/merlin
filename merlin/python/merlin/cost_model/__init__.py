"""Standalone Gemmini hardware cost model + calibration (GemminiCostModel, calibrate CLI).

Standalone tool (run: python -m merlin.cost_model.calibrate); the generic DSE cost function is
merlin.dse.cost_model.evaluate_cost.
"""
