"""Search layer over candidate compiler artifacts. Three methods only: grid (explicit sweeps), evolutionary (improve a candidate), MAP-Elites (keep many good families). Orchestration/experiment logic — NOT a dialect.

Scaffold package. No real logic yet.
"""
