// Minimal XNNPACK vunary.h shim: the vgelu/vsigmoid/vtanh RVV kernels only
// #include this for the param struct; the prototype is provided by the kernel
// source itself. Pull in the param definitions.
#ifndef MERLIN_CEILING_XNN_VUNARY_H
#define MERLIN_CEILING_XNN_VUNARY_H

#include "src/xnnpack/microparams.h"

#endif  // MERLIN_CEILING_XNN_VUNARY_H
