# example_vector (reference target)

A minimal placeholder vector target. Documentation/schema only — used to show that the
target layout generalizes beyond ToyNPU. Flesh out with a `target_contract.yaml` and
`docs/` when needed. See `docs/adding_a_target.md`.
