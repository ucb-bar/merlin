# SY_model_tiny_llama

SY_model_tiny_llama: model over I0[1, 8]:i64, authored from synthesized for the roster axis: whole model 'tiny_llama' at int8, the highest-ranked precision this target's manifest admits for a contraction out of the declared preference ['int8', 'fp8_e4m3', 'mxfp8', 'fp16', 'bf16', 'fp32'], captured with int8_dyn_act_int8_weight so the program contains the target's own arithmetic rather than a float matmul over dequantized weights. Accuracy in that format is not_established.

kind=model label=public op=model modes={}
