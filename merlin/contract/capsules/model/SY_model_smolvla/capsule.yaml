name: SY_model_smolvla
kind: model
source_role: pytorch_model_slice
source_reference: 'synthesized for the roster axis: whole model ''smolvla'' at int8,
  the highest-ranked precision this target''s manifest admits for a contraction out
  of the declared preference [''int8'', ''fp8_e4m3'', ''mxfp8'', ''fp16'', ''bf16'',
  ''fp32''], captured with int8_dyn_act_int8_weight so the program contains the target''s
  own arithmetic rather than a float matmul over dequantized weights. Accuracy in
  that format is not_established'
label: public
interface_mlir: capsule.interface.mlir
inputs:
- name: I0
  role: input
  shape:
  - 1
  - 3
  - 512
  - 512
  dtype: f32
- name: I1
  role: input
  shape:
  - 1
  dtype: i1
- name: I2
  role: input
  shape:
  - 1
  - 48
  dtype: i64
- name: I3
  role: input
  shape:
  - 1
  - 48
  dtype: i1
- name: I4
  role: input
  shape:
  - 1
  - 32
  dtype: f32
- name: I5
  role: input
  shape:
  - 1
  - 50
  - 32
  dtype: f32
operation:
  op: model
  attributes:
    model: smolvla
    dtype: i8
    out: Y0
    bundle_abi_version: 2
    quant_scheme: int8_dyn_act_int8_weight
    torch_seed: 0
    compile_dtype: int8
    arg_order:
    - I0
    - I1
    - I2
    - I3
    - I4
    - I5
    - Y0
    weights: capsule.weights.safetensors
    weights_manifest: capsule.weights.safetensors.manifest.json
numeric_policy:
  compare: tolerance_float
  dtype: f32
  atol: 0.03125
  rtol: 0.02
expected:
  instruction_classes:
  - FLUSH
  - CONFIG_EX
  - CONFIG_LD
  - MVIN
  - CONFIG_ST
  - PRELOAD
  - COMPUTE_PRELOADED
  - MVOUT
required_oracle_tiers:
- L0
- L1
- L2
- L3
vcs: optional
firesim: optional
gate:
  after_op_pass_fraction: 0.8
lanes:
  require:
  - on_mesh
pytorch_ref:
  op: model
  dtype: i8
  loader: capsule.pytorch.py
input_provenance:
  workload: smolvla
  status: undeclared
  synthetic_inputs: unknown
  paper_ready: unknown
  accuracy_claim_supported: false
  accuracy_claim_withheld_because: the loader declares no input provenance (status
    'undeclared'), so whether these inputs are real dataset samples is UNKNOWN --
    an unknown may not be quoted as either
  grades: compiler_correctness
  note: 'This capsule grades COMPILER CORRECTNESS: whether the compiled program reproduces,
    on these exact inputs, the reference the same loader produced. It is not a statement
    about the model''s accuracy on real data; only a capture whose inputs are real
    and attributed can support that.'
  loader_env: {}
  declared: {}
linalg_mlir: capsule.interface.mlir
semantic:
  semantic_family: contraction
  generalization_axis: roster
  must_accelerate: false
  not_asserted_reason: 'interop capsule: composition across lanes is the behaviour
    under test, so host-lane work is expected and required. The accelerator demand
    is expressed as lanes.require, which fails unless every named lane actually carried
    work.'
  eligible: auto
