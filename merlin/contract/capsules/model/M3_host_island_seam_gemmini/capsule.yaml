name: M3_host_island_seam_gemmini
kind: model
source_role: pytorch_model_slice
source_reference: 'int8 GEMM -> host LayerNorm -> int8 GEMM: the A->H->A seam as the
  subject of a capsule rather than a by-product of a whole model'
label: public
pass_requirements:
- model-boundary-capstone
interface_mlir: capsule.interface.mlir
inputs:
- name: I0
  role: input
  shape:
  - 16
  - 32
  dtype: i64
operation:
  op: model
  attributes:
    model: host_island_seam
    dtype: i8
    out: Y0
    compile_dtype: int8
    arg_order:
    - I0
    - Y0
    weights: capsule.weights.safetensors
numeric_policy:
  compare: tolerance_float
  dtype: f32
  atol: 0.03125
  rtol: 0.02
expected:
  instruction_classes:
  - FLUSH
  - CONFIG_EX
  - CONFIG_LD
  - MVIN
  - CONFIG_ST
  - PRELOAD
  - COMPUTE_PRELOADED
  - MVOUT
required_oracle_tiers:
- L0
- L1
- L2
- L3
vcs: optional
firesim: optional
gate:
  after_op_pass_fraction: 0.0
pytorch_ref:
  op: model
  dtype: i8
  loader: capsule.pytorch.py
linalg_mlir: capsule.interface.mlir
semantic:
  semantic_family: contraction
  generalization_axis: model
  must_accelerate: true
  eligible: auto
