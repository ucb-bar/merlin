# GX0_interop_rvv_lane

GX0_interop_rvv_lane: model over I0[1, 8]:i64, authored from small_llama int8 composed across the systolic mesh and the host scalar/RVV lane — asserts both lanes carried work.

kind=model label=public op=model modes={}
