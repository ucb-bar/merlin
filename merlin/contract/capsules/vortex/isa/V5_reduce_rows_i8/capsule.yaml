name: V5_reduce_rows_i8
kind: isa
source_role: handauthored_compiler_test
label: public
interface_mlir: capsule.interface.mlir
source_torch: capsule.torch.py
inputs:
- name: A
  role: input
  shape:
  - 8
  - 16
  dtype: i8
- name: Y
  role: output
  shape:
  - 8
  dtype: i32
operation:
  op: reduce
  attributes:
    lhs: A
    out: Y
    output_dtype: i32
    axis: 1
    k: 16
expected:
  instruction_classes:
  - CTA_CSR
  - GMEM_LD
  - GMEM_ST
  - TMC
  - WSPAWN
  modes:
    i8: true
    reduction: true
    divergent: false
required_oracle_tiers:
- L2
- L3
notes: the exact-integer reduction; cols=16 cannot overflow i32
numeric_policy:
  compare: exact_int
  dtype: i32
