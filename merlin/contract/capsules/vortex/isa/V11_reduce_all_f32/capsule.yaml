name: V11_reduce_all_f32
kind: isa
source_role: handauthored_compiler_test
label: public
interface_mlir: capsule.interface.mlir
source_torch: capsule.torch.py
inputs:
- name: A
  role: input
  shape:
  - 16
  - 16
  dtype: f32
- name: Y
  role: output
  shape:
  - 1
  dtype: f32
operation:
  op: reduce
  attributes:
    lhs: A
    out: Y
    output_dtype: f32
    axis: all
    reduce_op: sum
    k: 256
expected:
  instruction_classes:
  - CTA_CSR
  - GMEM_LD
  - GMEM_ST
  - TMC
  - WSPAWN
  - SHFL_DOWN
  - SHFL_BFLY
  - BARRIER
  - SMEM_LD
  - SMEM_ST
  modes:
    f32: true
    reduction: true
    cross_thread: true
    shared_mem: true
    divergent: false
required_oracle_tiers:
- L2
- L3
notes: 'the WHOLE tensor to one scalar: the corpus''s only reduction that cannot be
  split one-output-per-thread, so lanes must combine partial results'
numeric_policy:
  compare: tolerance_float
  dtype: f32
  rtol: 0.0
  atol: 0.003996384359382879
