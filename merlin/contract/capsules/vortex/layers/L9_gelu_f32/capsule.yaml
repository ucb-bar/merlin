name: L9_gelu_f32
kind: layer
source_role: handauthored_compiler_test
label: public
interface_mlir: capsule.interface.mlir
source_torch: capsule.torch.py
inputs:
- name: A
  role: input
  shape:
  - 16
  - 64
  dtype: f32
- name: Y
  role: output
  shape:
  - 16
  - 64
  dtype: f32
operation:
  op: elementwise
  attributes:
    lhs: A
    out: Y
    output_dtype: f32
    fn: gelu_tanh
expected:
  instruction_classes:
  - CTA_CSR
  - GMEM_LD
  - GMEM_ST
  - TMC
  - WSPAWN
  modes:
    f32: true
    elementwise: true
    gelu: true
    divergent: false
required_oracle_tiers:
- L2
- L3
notes: 'a transcendental with NO reduction attached: with L8 it brackets softmax,
  so a softmax failure can be attributed to the approximation or to the two-pass reduction
  instead of to ''softmax'''
numeric_policy:
  compare: tolerance_float
  dtype: f32
  rtol: 0.0
  atol: 2.2019269828264038e-06
