name: OPR2_k_1
kind: isa
source_role: handauthored_compiler_test
source_reference: 'a single rank-1 update: the accumulate fires exactly once, so a
  kernel that folds the readout into the loop tail emits it in the wrong place'
label: public
interface_mlir: capsule.interface.mlir
inputs:
- name: W
  role: weight
  shape:
  - 1
  - 32
  dtype: i8
- name: A0
  role: input
  shape:
  - 32
  - 1
  dtype: i8
operation:
  op: matmul
  attributes:
    lhs: A0
    weight: W
    out: Y0
    epilogue: []
    output_dtype: i32
numeric_policy:
  compare: exact_int
  dtype: i32
expected:
  instruction_classes:
  - OPMVIN
  - OPMVINBCAST
  - OPMACC
  - OPMVOUT
  modes:
    relu: false
    acc_scale: false
    i8: false
required_oracle_tiers:
- L3
vcs: optional
firesim: optional
semantic:
  semantic_family: contraction
  generalization_axis: seen
  must_accelerate: true
  eligible: auto
