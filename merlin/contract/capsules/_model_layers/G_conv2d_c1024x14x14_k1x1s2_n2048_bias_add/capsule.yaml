name: G_conv2d_c1024x14x14_k1x1s2_n2048_bias_add
kind: layer
source_role: model_derived
source_reference: '1 compute group(s) of resnet50 on gemmini: groups [70]'
label: dev
interface_mlir: capsule.interface.mlir
inputs:
- name: W
  role: weight
  shape:
  - 1024
  - 2048
  dtype: i8
- name: IFM
  role: input
  shape:
  - 1
  - 14
  - 14
  - 1024
  dtype: i8
- name: B
  role: bias
  shape:
  - 2048
  dtype: i32
operation:
  op: conv2d
  attributes:
    ifm: IFM
    weight: W
    out: Y0
    ci: 1024
    kh: 1
    kw: 1
    stride:
    - 2
    - 2
    padding:
    - 0
    - 0
    - 0
    - 0
    dilation:
    - 1
    - 1
    layout: nhwc
    epilogue:
    - bias_add
    output_dtype: i32
    semantic: conv2d_im2col
    bias: B
numeric_policy:
  compare: exact_int
  dtype: i32
expected:
  instruction_classes:
  - FLUSH
  - CONFIG_EX
  - CONFIG_LD
  - MVIN
  - CONFIG_ST
  - PRELOAD
  - COMPUTE_PRELOADED
  - MVOUT
  modes:
    conv2d: true
    k_accumulate: true
required_oracle_tiers:
- L0
- L1
- L2
- L3
# CAPPED, NOT CERTIFIED. This member's operands exceed what a cycle-accurate certification can
# afford, so declaring L3 for itself made it unverifiable by construction: no sibling can pass a
# tier deeper than the one it already claims. It is an EXTENSION of a certified sibling --
# `phase_policy.anchors` derives which one witnesses this obligation, so naming it here would
# either duplicate that answer or contradict it. The guarantee is the sibling's; this member
# carries the real shape.
max_oracle_tier: L2
vcs: optional
firesim: optional
stimulus_range:
- -4
- 3
semantic:
  semantic_family: contraction
  generalization_axis: composition
  must_accelerate: true
  eligible: auto
  composed_families:
  - elementwise_map
