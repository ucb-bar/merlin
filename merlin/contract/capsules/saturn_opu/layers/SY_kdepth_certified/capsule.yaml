name: SY_kdepth_certified
kind: layer
source_role: derived_sweep
source_reference: 'synthesized for the accumulation-depth axis: 2 accumulation passes,
  the shallowest reduction that writes the accumulator more than once. Emitted only
  because this target''s residency regimes yield no depth of their own; where they
  do, they produce deeper capsules and this would duplicate one. Costs 81.0s against
  a 300.0s budget -- priced on the OUTPUT tile, which the reduction depth does not
  move'
label: public
interface_mlir: capsule.interface.mlir
inputs:
- name: W
  role: weight
  shape:
  - 32
  - 16
  dtype: i8
- name: A0
  role: input
  shape:
  - 16
  - 32
  dtype: i8
operation:
  op: matmul
  attributes:
    lhs: A0
    weight: W
    out: Y0
    epilogue: []
    output_dtype: i32
    semantic:
      generalization_axis: accumulation_depth
numeric_policy:
  compare: exact_int
  dtype: i32
expected:
  instruction_classes: []
  modes: {}
required_oracle_tiers:
- L3
vcs: optional
firesim: optional
semantic:
  semantic_family: contraction
  generalization_axis: composition
  must_accelerate: false
  eligible: auto
pass_requirements:
- target-isa-lowering
- tile-schedule
