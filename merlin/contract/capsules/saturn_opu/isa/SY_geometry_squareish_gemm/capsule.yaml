name: SY_geometry_squareish_gemm
kind: isa
source_role: derived_sweep
source_reference: 'synthesized for geometry class ''squareish_gemm'': real captures
  present 58 contraction region(s) of this aspect ratio carrying 0.001016 of all contraction
  MAC work, and the heaviest of them is M=196 K=1024 N=256. Every other synthesized
  capsule is square, so without this the corpus cannot tell a compiler that tiles
  this ratio well from one that does not. 50176 written output elements exceeds the
  862 a 300.0s certification budget affords, and this target declares no tier cheaper
  than its cert tier (none resolved) to fall back to, so the capsule is emitted UNCAPPED
  and is expected to be expensive'
label: public
interface_mlir: capsule.interface.mlir
inputs:
- name: W
  role: weight
  shape:
  - 1024
  - 256
  dtype: i8
- name: A0
  role: input
  shape:
  - 196
  - 1024
  dtype: i8
operation:
  op: matmul
  attributes:
    lhs: A0
    weight: W
    out: Y0
    epilogue: []
    output_dtype: i32
    semantic:
      generalization_axis: shape_geometry
numeric_policy:
  compare: exact_int
  dtype: i32
expected:
  instruction_classes: []
  modes: {}
required_oracle_tiers:
- L3
vcs: optional
firesim: optional
semantic:
  semantic_family: contraction
  generalization_axis: seen
  must_accelerate: false
  eligible: auto
pass_requirements:
- target-isa-lowering
- tile-schedule
