# M3_spectformer_fp32

M3_spectformer_fp32: model over I0[1, 3, 224, 224]:f32, authored from spectformer — ViT (patch-embed + spectral/attention blocks + classifier head); matches the _vision e2e set.

kind=model label=public op=model modes={}
