name: MX1_interop_rvv_lane
kind: model
source_role: pytorch_model_slice
source_reference: "small_llama composed across the SIMT accelerator and the scalar/RVV\
  \ host lane \u2014 reuses the M0 capture, asserts both lanes carried work"
label: public
interface_mlir: capsule.interface.mlir
inputs:
- name: I0
  role: input
  shape:
  - 1
  - 8
  dtype: i64
operation:
  op: model
  attributes:
    model: small_llama
    dtype: f32
    out: Y0
    compile_dtype: fp32
    arg_order:
    - I0
    - Y0
    weights: capsule.weights.safetensors
numeric_policy:
  compare: tolerance_float
  dtype: f32
  atol: 0.03125
  rtol: 0.015625
expected:
  instruction_classes: []
required_oracle_tiers:
- L0
- L1
- L2
vcs: optional
firesim: optional
gate:
  after_op_pass_fraction: 0.8
lanes:
  require:
  - on_mesh
  - scalar_rvv_lane
pytorch_ref:
  op: model
  dtype: f32
  loader: capsule.pytorch.py
linalg_mlir: capsule.interface.mlir
semantic:
  semantic_family: contraction
  generalization_axis: model
  must_accelerate: false
  eligible: auto
  not_asserted_reason: the accelerator demand could not be derived from this model's
    linalg against this target's declared capabilities and role census, so must_accelerate
    is withheld rather than asserted ungrounded
