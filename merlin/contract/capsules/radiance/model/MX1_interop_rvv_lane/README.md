# MX1_interop_rvv_lane

MX1_interop_rvv_lane: model over I0[1, 8]:i64, authored from small_llama composed across the SIMT accelerator and the scalar/RVV host lane — reuses the M0 capture, asserts both lanes carried work.

kind=model label=public op=model modes={}
