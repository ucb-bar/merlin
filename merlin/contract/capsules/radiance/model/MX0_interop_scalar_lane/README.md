# MX0_interop_scalar_lane

MX0_interop_scalar_lane: model over I0[1, 8]:i64, authored from small_llama composed across the SIMT accelerator and the in-contract scalar/vector lane — reuses the M0 capture, asserts both lanes carried work.

kind=model label=public op=model modes={}
