# SY_regime_fits_double

SY_regime_fits_double: matmul over W[16, 16]:bf16, A0[16, 16]:bf16, authored from synthesized for memory regime 'fits_double': the declared inputs occupy 32 of 4096 operand-store rows (0.007812 of capacity), which is what puts this capsule in that regime. Extents resolved by memory_regime.extents_for_regime with the same sizing the coverage gate measures with.

kind=isa label=public op=matmul modes={}
