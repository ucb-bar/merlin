# SY_softmax_fp16_aligned

SY_softmax_fp16_aligned: softmax over X[16, 32]:fp16, authored from synthesized for conformance cell softmax/fp16/aligned (basis=observed_via_primitives, admitted_by=['simt_cluster']); extents from boundaries.extent_probes.

kind=isa label=public op=softmax modes={}
