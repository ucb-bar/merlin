name: SY_host_lane_reduction_f32
kind: model_slice
source_role: pytorch_model_slice
source_reference: 'synthesized for the host lane: real captures contain 2262 ''reduction''
  region(s) at f32, and this target''s manifest admits ''reduction'' at no such dtype
  -- so every one of them must be placed on the host. A corpus with no capsule here
  cannot tell a compiler that routes them correctly from one that does not'
label: public
interface_mlir: capsule.interface.mlir
inputs:
- name: X
  role: input
  shape:
  - 16
  - 32
  dtype: f32
operation:
  op: reduce_sum
  attributes:
    out: Y0
    arg_order:
    - X
    causal: false
numeric_policy:
  compare: tolerance_float
  dtype: f32
  atol: 0.03125
  rtol: 0.02
expected:
  instruction_classes: []
  modes: {}
required_oracle_tiers:
- L0
- L1
- L2
- L3
vcs: optional
firesim: optional
pytorch_ref:
  op: reduce_sum
  dtype: f32
  loader: capsule.pytorch.py
linalg_mlir: capsule.interface.mlir
semantic:
  semantic_family: reduction
  generalization_axis: composition
  must_accelerate: false
  eligible: auto
pass_requirements:
- host-seam
- target-isa-lowering
- tile-schedule
lanes:
  forbid:
  - on_mesh
numeric_falsifiability:
  status: ok
  declared_atol: 0.03125
  ceiling_atol: 6.187276706695557
  basis: the largest atol under which the best constant answer still fails this golden
  falsifiable: true
