name: SY_host_lane_attention_bf16
kind: model_slice
source_role: pytorch_model_slice
source_reference: 'synthesized for the host lane: real captures contain 6 ''attention''
  region(s) at bf16, and this target''s manifest admits ''attention'' at no such dtype
  -- so every one of them must be placed on the host. A corpus with no capsule here
  cannot tell a compiler that routes them correctly from one that does not'
label: public
interface_mlir: capsule.interface.mlir
inputs:
- name: Q
  role: input
  shape:
  - 16
  - 32
  dtype: bf16
- name: K
  role: input
  shape:
  - 16
  - 32
  dtype: bf16
- name: V
  role: input
  shape:
  - 16
  - 32
  dtype: bf16
operation:
  op: attention_full
  attributes:
    out: Y0
    arg_order:
    - Q
    - K
    - V
    causal: false
numeric_policy:
  compare: tolerance_float
  dtype: bf16
  atol: 0.03125
  rtol: 0.02
expected:
  instruction_classes: []
  modes: {}
required_oracle_tiers:
- L0
- L1
- L2
- L3
vcs: optional
firesim: optional
pytorch_ref:
  op: attention_full
  dtype: bf16
  loader: capsule.pytorch.py
linalg_mlir: capsule.interface.mlir
semantic:
  semantic_family: attention
  generalization_axis: host_lane
  must_accelerate: false
  not_asserted_reason: the target admits no datapath for this family at this dtype;
    the capsule exists to prove the compiler does NOT accelerate it
  eligible: false
pass_requirements:
- host-seam
- target-isa-lowering
- tile-schedule
lanes:
  forbid:
  - on_mesh
numeric_falsifiability:
  status: ok
  declared_atol: 0.03125
  ceiling_atol: 0.389375
  basis: the largest atol under which the best constant answer still fails this golden
  falsifiable: true
