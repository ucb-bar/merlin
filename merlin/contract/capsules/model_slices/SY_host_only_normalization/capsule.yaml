name: SY_host_only_normalization
kind: model_slice
source_role: pytorch_model_slice
source_reference: 'synthesized for host-only family ''normalization'': real captures
  contain it and this target''s capability manifest admits no capability for it, so
  the compiler must leave it on the host lane. dtype f32 is the one the captures carry
  for this family'
label: public
interface_mlir: capsule.interface.mlir
inputs:
- name: X
  role: input
  shape:
  - 16
  - 32
  dtype: f32
- name: G
  role: weight
  shape:
  - 1
  - 32
  dtype: f32
operation:
  op: rmsnorm
  attributes:
    src: X
    gamma: G
    out: Y0
    eps: 1.52587890625e-05
    semantic: rmsnorm
    output_dtype: f32
numeric_policy:
  compare: tolerance_float
  dtype: f32
expected:
  instruction_classes: []
  modes:
    rmsnorm: true
required_oracle_tiers:
- L0
- L1
- L2
- L3
vcs: optional
firesim: optional
semantic:
  semantic_family: normalization
  generalization_axis: composition
  must_accelerate: true
  eligible: auto
pytorch_ref:
  op: rmsnorm
  dtype: f32
  loader: capsule.pytorch.py
linalg_mlir: capsule.linalg.mlir
pass_requirements:
- host-seam
- target-isa-lowering
- tile-schedule
lanes:
  forbid:
  - on_mesh
