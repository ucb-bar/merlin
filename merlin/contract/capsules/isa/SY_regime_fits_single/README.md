# SY_regime_fits_single

SY_regime_fits_single: matmul over W[16, 16]:i8, A0[8192, 16]:i8, authored from synthesized for memory regime 'fits_single': the declared inputs occupy 8208 of 16384 operand-store rows (0.500977 of capacity), which is what puts this capsule in that regime. Extents resolved by memory_regime.extents_for_regime with the same sizing the coverage gate measures with.

kind=isa label=public op=matmul modes={}
