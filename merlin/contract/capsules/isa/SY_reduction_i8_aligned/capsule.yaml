name: SY_reduction_i8_aligned
kind: isa
source_role: derived_sweep
source_reference: synthesized for conformance cell reduction/i8/aligned (basis=observed,
  admitted_by=['systolic_mesh']); extents from boundaries.extent_probes; reduction
  is admitted only in composition with ['contraction', 'movement'], so it is carried
  as a fused epilogue on matmul -- a standalone capsule for it would be refused by
  the eligibility oracle as a false fallback
label: public
interface_mlir: capsule.interface.mlir
inputs:
- name: W
  role: weight
  shape:
  - 32
  - 16
  dtype: i8
- name: A0
  role: input
  shape:
  - 16
  - 32
  dtype: i8
operation:
  op: matmul
  attributes:
    lhs: A0
    weight: W
    out: Y0
    epilogue:
    - maxpool
    output_dtype: i32
    pool_in_dims:
    - 4
    - 4
    pool_size:
    - 2
    - 2
    pool_stride:
    - 2
    - 2
    pool_padding:
    - 0
    - 0
    - 0
    - 0
numeric_policy:
  compare: exact_int
  dtype: i32
expected:
  instruction_classes:
  - FLUSH
  - CONFIG_EX
  - CONFIG_LD
  - MVIN
  - CONFIG_ST
  - PRELOAD
  - COMPUTE_PRELOADED
  - MVOUT
  modes:
    relu: false
    acc_scale: false
    i8: false
required_oracle_tiers:
- L0
- L1
- L2
- L3
vcs: optional
firesim: optional
semantic:
  semantic_family: contraction
  generalization_axis: seen
  must_accelerate: true
  eligible: auto
  composed_families:
  - reduction
pass_requirements:
- target-isa-lowering
- tile-schedule
