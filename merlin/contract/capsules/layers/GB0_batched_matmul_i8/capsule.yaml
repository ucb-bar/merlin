name: GB0_batched_matmul_i8
kind: layer
source_role: derived_sweep
source_reference: 'batched int8 contraction: two independent (M,H)x(H,N) slices, the
  rank-3 shape the target contract admits and no other capsule declares'
label: public
interface_mlir: capsule.interface.mlir
inputs:
- name: W
  role: weight
  shape:
  - 2
  - 32
  - 16
  dtype: i8
- name: A0
  role: input
  shape:
  - 2
  - 16
  - 32
  dtype: i8
operation:
  op: gemv_batched
  attributes:
    lhs: A0
    weight: W
    out: Y0
    batch: 2
    semantic: gemv_batched
    output_dtype: i32
numeric_policy:
  compare: exact_int
  dtype: i32
expected:
  instruction_classes:
  - FLUSH
  - CONFIG_EX
  - CONFIG_LD
  - MVIN
  - CONFIG_ST
  - PRELOAD
  - COMPUTE_PRELOADED
  - MVOUT
  modes: {}
required_oracle_tiers:
- L0
- L1
- L2
- L3
vcs: optional
firesim: optional
semantic:
  semantic_family: contraction
  generalization_axis: composition
  must_accelerate: true
  eligible: auto
