name: GP1_matmul_maxpool_tail_i8
kind: layer
source_role: handauthored_compiler_test
source_reference: 'derived pooling coverage at the tail: a 5x5 output plane pooled
  by a 2x2/2 window (the fifth row and column fall outside every window and must be
  DROPPED, not folded in) with an output extent that does not divide the array edge
  -- the `reduction/i8/partial` cell'
label: public
interface_mlir: capsule.interface.mlir
inputs:
- name: W
  role: weight
  shape:
  - 16
  - 17
  dtype: i8
- name: A0
  role: input
  shape:
  - 25
  - 16
  dtype: i8
operation:
  op: matmul
  attributes:
    lhs: A0
    weight: W
    out: Y0
    epilogue:
    - maxpool
    output_dtype: i32
    pool_in_dims:
    - 5
    - 5
    pool_size:
    - 2
    - 2
    pool_stride:
    - 2
    - 2
    pool_padding:
    - 0
    - 0
    - 0
    - 0
numeric_policy:
  compare: exact_int
  dtype: i32
expected:
  instruction_classes:
  - FLUSH
  - CONFIG_EX
  - CONFIG_LD
  - MVIN
  - CONFIG_ST
  - PRELOAD
  - COMPUTE_PRELOADED
  - MVOUT
  modes:
    i8: true
    relu: false
    acc_scale: false
required_oracle_tiers:
- L0
- L1
- L2
- L3
vcs: optional
firesim: optional
semantic:
  semantic_family: contraction
  generalization_axis: composition
  must_accelerate: true
  eligible: auto
  composed_families:
  - reduction
