name: SY_host_lane_elementwise_map_f32
kind: model_slice
source_role: pytorch_model_slice
source_reference: 'synthesized for the host lane: real captures contain 8392 ''elementwise_map''
  region(s) at f32, and this target''s manifest admits ''elementwise_map'' at no such
  dtype -- so every one of them must be placed on the host. A corpus with no capsule
  here cannot tell a compiler that routes them correctly from one that does not'
label: public
interface_mlir: capsule.interface.mlir
inputs:
- name: X
  role: input
  shape:
  - 32
  - 64
  dtype: f32
operation:
  op: gelu
  attributes:
    out: Y0
    arg_order:
    - X
    causal: false
numeric_policy:
  compare: tolerance_float
  dtype: f32
  atol: 0.25
  rtol: 0.02
expected:
  instruction_classes:
  - TensorBaseOffset
  - TensorComputeBinary
  modes: {}
required_oracle_tiers:
- L0
- L1
- L2
- L3
vcs: optional
firesim: optional
pytorch_ref:
  op: gelu
  dtype: f32
  loader: capsule.pytorch.py
linalg_mlir: capsule.interface.mlir
semantic:
  semantic_family: elementwise_map
  generalization_axis: composition
  must_accelerate: false
  eligible: auto
pass_requirements:
- host-seam
- target-isa-lowering
- tile-schedule
lanes:
  forbid:
  - on_mesh
numeric_falsifiability:
  status: ok
  declared_atol: 0.25
  ceiling_atol: 0.5021220707893371
  basis: the largest atol under which the best constant answer still fails this golden
  falsifiable: true
