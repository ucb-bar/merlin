# AX0_interop_vector_unit

AX0_interop_vector_unit: model over I0[1, 8]:i64, authored from small_llama fp8 composed across the mxu and the in-contract vector unit — asserts both declared units carried work.

kind=model label=public op=model modes={}
