# AX1_interop_rvv_lane

AX1_interop_rvv_lane: model over I0[1, 8]:i64, authored from small_llama fp8 composed across the mxu and the host scalar/RVV lane — asserts work left the accelerator entirely and stayed correct.

kind=model label=public op=model modes={}
