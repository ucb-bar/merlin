# Integration manifest for xDSL.
name: xdsl
display_name: xDSL
kind: adapter
vendored: false
external_repo:
  env_var: MERLIN_XDSL_REPO
  required: false
emits_schema: n/a
status: scaffold
notes: >-
  External repo is passed by path/env var, never vendored. Adapter must degrade
  gracefully when MERLIN_XDSL_REPO is unset.
