# Integration manifest for IREE.
name: iree
display_name: IREE
kind: adapter
vendored: false
external_repo:
  env_var: MERLIN_IREE_REPO
  required: false
emits_schema: n/a
status: scaffold
notes: >-
  External repo is passed by path/env var, never vendored. Adapter must degrade
  gracefully when MERLIN_IREE_REPO is unset.
