# Integration manifest for Exo.
name: exo
display_name: Exo
kind: adapter
vendored: false
external_repo:
  env_var: MERLIN_EXO_REPO
  required: false
emits_schema: kernel_record
status: scaffold
notes: >-
  External repo is passed by path/env var, never vendored. Adapter must degrade
  gracefully when MERLIN_EXO_REPO is unset.
