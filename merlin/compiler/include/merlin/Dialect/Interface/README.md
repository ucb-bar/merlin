# Interface dialect (headers) — SCAFFOLD

interface — target-independent HW/SW abstractions: resident tensors, accumulators, streaming tiles, event tokens, command objects. The central research dialect.

No ops/types implemented yet. Prototype this dialect in xDSL first:
`merlin/python/merlin/xdsl_dialects/interface.py`. Promote here once stable.
See `docs/dialects.md`.
